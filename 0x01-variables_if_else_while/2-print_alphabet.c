#include <stdio.h>
/**
 * main - print alpha
 * Return: Always 0 (success)
 */

int main(void)
{
	char str[25];
	int i;

	for (i=0; i < 26; i++)
	{
		putchar(str[i]);
	}
	putchar('\n');
	return (0);
}
